/** Notes a non-premium author can create before needing the $1 upgrade. */
export const FREE_NOTE_LIMIT = 5;

/** Longest a single note may be, enforced on both the composer and the API. */
export const MAX_NOTE_LENGTH = 2000;

/** Notes shown per page on the author dashboard. */
export const NOTES_PER_PAGE = 5;

/** One-time upgrade price in cents. */
export const UPGRADE_PRICE_CENTS = 100;
